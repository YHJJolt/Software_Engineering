<%@ Master Language="C#" AutoEventWireup="true" CodeBehind="StudentMaster.master.cs" Inherits="SchoolSystem.StudentMaster" %>

<!DOCTYPE html>
<html>
<head runat="server">
    <meta charset="utf-8" />
    <title>Student Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" />
    <link rel="stylesheet" type="text/css" href="~/Student/Student.css" />
    <asp:ContentPlaceHolder ID="head" runat="server"></asp:ContentPlaceHolder>
</head>
<body>
    <form id="form1" runat="server">

        <asp:ScriptManager ID="ScriptManager1" runat="server" EnablePageMethods="true" />

        <div id="sidebar">
            <div class="sidebar-header">
                <i class="fas fa-user-graduate icon"></i>
                <span>Student Panel</span>
            </div>
            
            <div class="nav-links">
                <a id="linkDashboard" runat="server" href="~/Student/StudentDashboard.aspx" class="nav-link"><i class="fas fa-home"></i> <span>Dashboard</span></a>
                <a id="linkCourses" runat="server" href="~/Student/StudentCourses.aspx" class="nav-link"><i class="fas fa-book"></i> <span>Courses</span></a>
                <a id="linkAttendance" runat="server" href="~/Student/StudentAttendance.aspx" class="nav-link"><i class="fas fa-user-check"></i> <span>View Attendance</span></a>
                <a id="linkGrades" runat="server" href="~/Student/StudentAllGrades.aspx" class="nav-link"><i class="fas fa-graduation-cap"></i> <span>Grades</span></a>
                <a id="linkCalendar" runat="server" href="~/Student/StudentCalendar.aspx" class="nav-link"><i class="fas fa-calendar-alt"></i> <span>Calendar</span></a>
                <a id="linkEnrollment" runat="server" href="~/Student/StudentEnrollments.aspx" class="nav-link"><i class="fas fa-plus-circle"></i> <span>Course Enrolment</span></a>
                <a id="linkPayment" runat="server" href="~/Student/StudentPayments.aspx" class="nav-link"><i class="fas fa-credit-card"></i> <span>Payment</span></a>
            </div>

            <a runat="server" href="~/Student/StudentProfile.aspx" class="sidebar-profile">
                <asp:Image ID="imgSidebar" runat="server" CssClass="p-img" ImageUrl="~/Images/default-avatar.png" />
                <div class="p-text">
                    <span class="p-name"><asp:Literal ID="litSidebarName" runat="server"></asp:Literal></span>
                </div>
            </a>
        </div>

        <div id="main-wrapper">
            <div id="topbar">
                <div class="topbar-left">
                    <button type="button" class="menu-toggle" onclick="toggleSidebar()">
                        <i class="fas fa-bars"></i>
                    </button>
                    <h2 class="page-title"><%: Page.Title %></h2>
                </div>
                
                <%-- NOTIFICATION BELL --%>
                <div class="notif-container">
                    <div class="notif-bell-wrapper" onclick="toggleNotifs(event)">
                        <i class="fas fa-bell notif-bell"></i>
                        <asp:Literal ID="litNotifCount" runat="server"></asp:Literal>
                    </div>

                    <div id="notifDropdown" class="notif-dropdown" style="display:none;">

                        <div class="notif-header">
                            <h4>Notifications</h4>
                            <asp:UpdatePanel ID="updMarkRead" runat="server" UpdateMode="Conditional">
                                <ContentTemplate>
                                    <asp:LinkButton ID="btnMarkRead" runat="server"
                                        CssClass="mark-read-btn"
                                        OnClick="btnMarkRead_Click"
                                        OnClientClick="handleMarkRead();">
                                        Mark all as read
                                    </asp:LinkButton>
                                </ContentTemplate>
                            </asp:UpdatePanel>
                        </div>

                        <div class="notif-body">
                            <asp:PlaceHolder ID="noNotifs" runat="server" Visible="false">
                                <div class="no-notifs">
                                    <i class="fas fa-bell-slash"></i>
                                    <p>You're all caught up!</p>
                                </div>
                            </asp:PlaceHolder>

                            <asp:Repeater ID="rptNotifications" runat="server">
                                <ItemTemplate>
                                    <a href='<%# Eval("Link") != DBNull.Value ? ResolveUrl(Eval("Link").ToString()) : "#" %>'
                                       class='notif-item <%# Convert.ToInt32(Eval("IsUnread")) == 1 ? "unread" : "" %>'>
                                        <div class="notif-icon-wrapper">
                                            <i class='<%# Eval("Type").ToString() == "Assignment Graded" ? "fas fa-check-circle" : Eval("Type").ToString() == "New Event" ? "fas fa-calendar-day" : Eval("Type").ToString() == "Event Reminder" ? "fas fa-clock" : Eval("Type").ToString() == "Enrollment Rejected" ? "fas fa-times-circle" : "fas fa-bullhorn" %>'></i>
                                        </div>
                                        <div class="notif-content">
                                            <span class="notif-msg"><%# Eval("Message") %></span>
                                            <span class="notif-time"><%# Eval("TimeAgo") %></span>
                                        </div>
                                    </a>
                                </ItemTemplate>
                            </asp:Repeater>
                        </div>
                    </div>
                </div>
            </div>

            <div id="main-content">
                <asp:ContentPlaceHolder ID="MainContent" runat="server"></asp:ContentPlaceHolder>
            </div>
        </div>
    </form>

    <script>
        function toggleSidebar() {
            document.body.classList.toggle('sidebar-collapsed');
            localStorage.setItem('sidebarState', document.body.classList.contains('sidebar-collapsed') ? 'collapsed' : 'expanded');
        }

        window.onload = function () {
            if (localStorage.getItem('sidebarState') === 'collapsed') {
                document.body.classList.add('sidebar-collapsed');
            }
        }

        function toggleNotifs(event) {
            if (event) event.stopPropagation();
            var dropdown = document.getElementById('notifDropdown');
            var isOpening = dropdown.style.display !== 'block';
            dropdown.style.display = isOpening ? 'block' : 'none';
            if (isOpening) {
                zeroOutBadge();
                var btn = document.getElementById('<%= btnMarkRead.ClientID %>');
                if (btn) btn.click();
            }
        }

        function zeroOutBadge() {
            var badge = document.querySelector('.notif-badge');
            if (badge) badge.style.display = 'none';

            document.querySelectorAll('.notif-item.unread').forEach(function (el) {
                el.classList.remove('unread');
            });
        }

        function handleMarkRead() {
            event.stopPropagation();
            zeroOutBadge();
        }

        document.getElementById('notifDropdown').addEventListener('click', function (e) {
            e.stopPropagation();
        });

        document.addEventListener('click', function (event) {
            var container = document.querySelector('.notif-container');
            if (container && !container.contains(event.target)) {
                var dd = document.getElementById('notifDropdown');
                if (dd) dd.style.display = 'none';
            }
        });
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>